import React, { FC } from "react";

const Dashboard: FC = () => {
   return (
      <section className="flex flex-col mt-2">
         <h1 className="text-[24px]">It's admin dashboard</h1>
      </section>
   );
};

export default Dashboard;
